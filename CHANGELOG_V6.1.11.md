# V6.1.11 名單操作與手機篩選優化

- 現任公職新增「職位優先、縣市優先、姓名排序」三種快速排序，並保留政黨分組。
- 新增政黨群組「全部展開／全部收合」，無黨籍仍固定置於名單最下方。
- 地圖滑過縣市或行政區時，左下角會同步顯示目前位置；進入縣市後持續標示地圖層級。
- 手機版篩選改為有遮罩的抽屜，新增「完成」按鈕、點擊外側關閉與 Esc 關閉。
- 手機名單工具列重新排版，避免搜尋、排序與操作按鈕互相擠壓。
- 本版只調整公開介面，不包含 `data/`，不會覆蓋已同步的官方資料。
